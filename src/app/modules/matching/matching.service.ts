import mongoose from 'mongoose';
import ApiError from '../../../errors/ApiErrors';
import httpStatus from 'http-status';

const matchingForFitter = async (userId: string) => {

};

export const matchingService = {
  matchingForFitter
};